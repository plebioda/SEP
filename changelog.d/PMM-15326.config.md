Added `host.docker.internal` to the default `ALLOWED_HOSTS` so pmm-managed can reach SEP from inside the pmm-server container, where no other name resolves. A deployment that overrides `ALLOWED_HOSTS` keeps its own list and must add the name itself, or TrustedHostMiddleware answers 400 to every cross-container request whatever the credentials.
